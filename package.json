{
  "name": "suichenBlog",
  "version": "1.0.0",
  "author": "reco_luan",
  "scripts": {
    "dev": "vuepress dev . --open --host \"localhost\"",
    "serve": "SET NODE_OPTIONS=--openssl-legacy-provider && vuepress dev . --open --host \"localhost\"",
    "build": "SET NODE_OPTIONS=--openssl-legacy-provider && vuepress build ."
  },
  "devDependencies": {
    "@vuepress-reco/vuepress-plugin-kan-ban-niang": "^1.0.5",
    "vuepress": "1.5.4",
    "vuepress-theme-reco": "1.5.7"
  },
  "dependencies": {
    "vue-class-component": "^7.2.3"
  },
  "description": "this is suichen blog"
}
